// Hero Banner variant
// The boilerplate hero block has no JavaScript decoration; content and layout
// are handled by section rendering and CSS. Kept as a no-op for consistency.
// eslint-disable-next-line no-unused-vars
export default function decorate(block) {}
